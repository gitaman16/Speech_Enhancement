# Core ML framework
tensorflow>=2.12.0

# Audio I/O
soundfile>=0.12.1

# Speech quality metrics
pystoi>=0.3.3
pesq>=0.0.4

# Scientific computing
numpy>=1.23.0
scipy>=1.9.0

# Visualization
matplotlib>=3.6.0

# Training progress bar
tqdm>=4.64.0


# ── For weight conversion only ──────────────────────────────────────────────
# Install PyTorch ONCE to run:  python weights/convert_weights.py
# Uninstall afterward if needed — it is not required for training or inference.
#
# CPU-only install (smaller download, sufficient for conversion):
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
#
# torch>=2.0.0
